apiVersion: v1
kind: Service
metadata:
    name: "demo-appisnno-svc"
    annotations:
        service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
    type: LoadBalancer
    # loadBalancerIP:
    ports:
    - port: 80
    selector:
        app: "appinno"
# apiVersion: v1
# kind: Service
# metadata:
#     name: "ghworkshop"
#     labels:
#         app: "ghworkshop"
# spec:
#     type: LoadBalancer
#     ports:
#     - port: 8090
#       targetPort: 8090
#       protocol: TCP
#       name: http
#     selector:
#         app: "ghworkshop"
